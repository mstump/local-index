---
phase: 10-ocr-providers
plan: 02
type: execute
wave: 1
depends_on:
  - 10-01
files_modified:
  - Cargo.toml
  - src/credentials.rs
  - src/error.rs
  - src/cli.rs
  - src/pipeline/assets/ocr.rs
  - src/pipeline/assets/document_ai.rs
  - src/pipeline/assets/mod.rs
  - src/main.rs
  - src/daemon/mod.rs
  - README.md
  - tests/document_ai_mock.rs
autonomous: true
requirements:
  - PRE-07
  - PRE-08

must_haves:
  truths:
    - "Operators can select Google Document AI for scanned PDF OCR via `LOCAL_INDEX_OCR_PROVIDER=google` (and matching CLI flag) when project/location/processor/credentials are set"
    - "Default remains Anthropic when env is unset; misconfiguration lists every missing Google setting in one error"
    - "README documents Anthropic vs Google switching, required env vars, and that standalone images still require `ANTHROPIC_API_KEY` when indexing images"
  artifacts:
    - path: "src/pipeline/assets/document_ai.rs"
      provides: "DocumentAiClient + process PNG page via REST :process"
      contains: "documentai.googleapis.com"
    - path: "README.md"
      provides: "OCR provider section"
      contains: "LOCAL_INDEX_OCR_PROVIDER"
  key_links:
    - from: "src/credentials.rs"
      to: "src/pipeline/assets/document_ai.rs"
      via: "service account / ADC token"
      pattern: "GOOGLE_APPLICATION_CREDENTIALS"
---

<threat_model>
**ASVS L1 scope (credentials + cloud API)**

| ID | Threat | Severity | Mitigation in this plan |
|----|--------|----------|---------------------------|
| T-10-02 | OAuth token leaked in logs | High | Never log bearer tokens or service account JSON contents |
| T-10-02b | SSRF via custom endpoints | Medium | Hard-code Document AI hostname `documentai.googleapis.com` (no user-controlled base URL) |
| T-10-02c | TLS | High | `reqwest` + rustls only |

**Block on:** High severity findings for token exposure.

</threat_model>

<objective>
Add **Google Document AI** as `OcrService::Google`, configuration surface (**env + CLI** on `index`/`daemon`), credential resolution aligned with PRE-14, wiremock integration test, and README documentation (PRE-07 completion for Google path; PRE-08 configurability).

Depends on **10-01** completing `OcrService` wiring for NeedsVision.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-ocr-providers/10-CONTEXT.md
@.planning/phases/10-ocr-providers/10-RESEARCH.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Config types + credential validation for Google</name>
  <files>src/credentials.rs, src/error.rs, src/pipeline/assets/document_ai.rs (skeleton)</files>
  <read_first>src/credentials.rs, src/error.rs, src/pipeline/assets/anthropic_extract.rs</read_first>
  <action>
Add resolver helpers (exact names up to implementer) that load:

- `GOOGLE_CLOUD_PROJECT` (non-empty string)
- `GOOGLE_DOCUMENT_AI_LOCATION` (e.g. `us` or `eu`)
- `GOOGLE_DOCUMENT_AI_PROCESSOR_ID` (processor ID string)

If `LOCAL_INDEX_OCR_PROVIDER` resolves to **google**, call a single validation function that returns `LocalIndexError::Credential` or `Config` with a message listing **every** missing variable name among: `GOOGLE_APPLICATION_CREDENTIALS` (or document ADC), `GOOGLE_CLOUD_PROJECT`, `GOOGLE_DOCUMENT_AI_LOCATION`, `GOOGLE_DOCUMENT_AI_PROCESSOR_ID`, and mention Google Cloud Console / Document AI processor setup (actionable, like Anthropic errors).

Implement OAuth2 **access token** acquisition from service account JSON using crates appropriate for the repo (e.g. `jsonwebtoken` + `reqwest` token POST, or a minimal maintained Google auth helper). **Do not** shell out to `gcloud`.

Expose `DocumentAiClient::new_from_env() -> Result<Self, LocalIndexError>` that fails fast when provider is google and any piece is missing.

Skeleton `document_ai.rs` with struct holding `project_id`, `location`, `processor_id`, `http: reqwest::Client`, and token source fields as needed.
  </action>
  <verify>
    <automated>cd /Users/matthewstump/src/local-index && cargo test -p local-index credentials 2>&1</automated>
  </verify>
  <acceptance_criteria>
- `rg "GOOGLE_DOCUMENT_AI" src/credentials.rs` or `rg "GOOGLE_DOCUMENT_AI" src/pipeline/assets/document_ai.rs` matches at least processor/location/project
- Unit test: with `LOCAL_INDEX_OCR_PROVIDER=google` and unset Google vars, error message contains `GOOGLE_CLOUD_PROJECT` and `GOOGLE_DOCUMENT_AI_PROCESSOR_ID` substrings (use env mutex pattern like `credentials.rs` tests)
- `cargo test -p local-index credentials` exits 0 (or scoped test name you add)
  </acceptance_criteria>
  <done>Google config fails fast withenumerated missing keys.</done>
</task>

<task type="auto">
  <name>Task 2: Document AI `process` implementation + `OcrService::Google`</name>
  <files>src/pipeline/assets/document_ai.rs, src/pipeline/assets/ocr.rs, Cargo.toml</files>
  <read_first>src/pipeline/assets/ocr.rs, src/pipeline/assets/document_ai.rs, Cargo.toml</read_first>
  <action>
Implement `POST https://{location}-documentai.googleapis.com/v1/projects/{project}/locations/{location}/processors/{processor}:process` with `Authorization: Bearer {token}`.

Request JSON (concrete shape in implementation): per page call or batch per RESEARCH — **minimum:** one `process` call per PNG page with `rawDocument` base64 `image/png` (match Google API field names exactly).

Parse response JSON: extract **`document.text`** (UTF-8). If empty, return `LocalIndexError::AssetVision` or `Config` with a clear message (grep-verifiable substring `document.text` or `empty OCR`).

Extend `OcrService` enum with `Google(DocumentAiClient)` and implement `ocr_scanned_pdf_pages` by dispatching: Anthropic path unchanged; Google path calls new client per page.

Add required dependencies to `Cargo.toml` with pinned versions.

Retry policy: optional single retry on 429 (match project patterns); document in code comment if omitted.
  </action>
  <verify>
    <automated>cd /Users/matthewstump/src/local-index && cargo test -p local-index --test document_ai_mock 2>&1</automated>
  </verify>
  <acceptance_criteria>
- `rg "documentai.googleapis.com" src/pipeline/assets/document_ai.rs` matches
- `rg "Google(" src/pipeline/assets/ocr.rs` matches enum variant
- `cargo test -p local-index --test document_ai_mock` exits 0
  </acceptance_criteria>
  <done>Google OCR works against wiremock.</done>
</task>

<task type="auto">
  <name>Task 3: CLI + env + wiring factory</name>
  <files>src/cli.rs, src/main.rs, src/daemon/mod.rs</files>
  <read_first>src/cli.rs, src/main.rs, src/daemon/mod.rs, src/pipeline/assets/ocr.rs</read_first>
  <action>
Add **ValueEnum** `OcrProviderCli { Anthropic, Google }` (or lowercase serde-friendly names matching env).

CLI flags on **`index`** and **`daemon`** subcommands only:

- `--ocr-provider <PROVIDER>` with `env = "LOCAL_INDEX_OCR_PROVIDER"` (document default: anthropic)

Precedence: CLI matches existing global/data_dir patterns — **subcommand-local** flags like `skip-asset-processing`.

Factory logic at startup:

1. Parse provider from env/CLI (CLI overrides env if clap configured that way — match project convention).

2. If **anthropic** (default): build `OcrService::Anthropic(client)` when key present; same as 10-01.

3. If **google**: build `DocumentAiClient`, wrap `OcrService::Google(...)`. **Standalone images** still require `AnthropicAssetClient::new_from_env()` separately — if images may be indexed and Anthropic key missing, keep existing image error.

Integration: daemon receives enough state to construct `OcrService` per process startup (same as main).

Update long help text in `cli.rs` for Index/Daemon mentioning PDF OCR vs images.
  </action>
  <verify>
    <automated>cd /Users/matthewstump/src/local-index && cargo run -p local-index -- index --help 2>&1 | head -80</automated>
  </verify>
  <acceptance_criteria>
- `rg "ocr-provider" src/cli.rs` matches
- `rg "LOCAL_INDEX_OCR_PROVIDER" src/cli.rs` matches
- `cargo run -p local-index -- index --help` stdout contains `ocr-provider` substring
- `cargo test -p local-index` exits 0
  </acceptance_criteria>
  <done>Operators can select provider from CLI/env.</done>
</task>

<task type="auto">
  <name>Task 4: wiremock `document_ai_mock` test + README</name>
  <files>tests/document_ai_mock.rs, README.md</files>
  <read_first>tests/anthropic_assets_mock.rs, README.md</read_first>
  <action>
Create `tests/document_ai_mock.rs`:

- Spin up `wiremock::MockServer`
- Stub **one** successful `:process` response with minimal JSON body including `"text":"OCR_PAGE_OK"` nested under `document` per actual parser (adjust mock to match serde structs).
- Assert `ocr_scanned_pdf_pages` or public wrapper returns non-empty strings containing `OCR_PAGE_OK`.

**README.md:** Add section **OCR providers (scanned PDFs)** listing:

- `LOCAL_INDEX_OCR_PROVIDER` values `anthropic` (default) and `google`
- Anthropic: `ANTHROPIC_API_KEY`
- Google: all required env vars + link to Document AI processors
- Note: **standalone images** still use Anthropic in Phase 10

Keep prose consistent with existing README asset-cache section.
  </action>
  <verify>
    <automated>cd /Users/matthewstump/src/local-index && cargo test -p local-index --test document_ai_mock 2>&1 && rg "LOCAL_INDEX_OCR_PROVIDER" README.md</automated>
  </verify>
  <acceptance_criteria>
- `tests/document_ai_mock.rs` exists
- `README.md` contains literal substring `LOCAL_INDEX_OCR_PROVIDER`
- `README.md` contains literal substring `GOOGLE_DOCUMENT_AI_PROCESSOR_ID`
- `cargo test -p local-index --test document_ai_mock` exits 0
  </acceptance_criteria>
  <done>PRE-08 documented; mock proves Google path.</done>
</task>

</tasks>

---

## PLANNING COMPLETE
