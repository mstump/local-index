---
phase: 04.1-sigterm-gap-closure
verified: 2026-04-10T23:00:00Z
status: human_needed
score: 5/5 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 4/5
  gaps_closed:
    - "SIGINT/SIGTERM triggers graceful shutdown that completes in-flight work"
  gaps_remaining: []
  regressions: []
human_verification:
  - test: "SIGTERM graceful shutdown end-to-end"
    expected: "Running 'kill <daemon_pid>' (which sends SIGTERM) against a live daemon logs 'shutdown signal received, draining in-flight work...' and the daemon exits cleanly with 'daemon shutdown complete'"
    why_human: "Requires a running daemon process with a real vault path and VOYAGE_API_KEY. Signal delivery to a live process cannot be tested with static analysis or in-process unit tests."
  - test: "SIGINT graceful shutdown (carried from Phase 04)"
    expected: "Pressing Ctrl-C once logs 'shutdown signal received, draining in-flight work...' then the process completes in-flight work and logs 'daemon shutdown complete'. A second Ctrl-C forces immediate exit."
    why_human: "Interactive signal delivery requires a running process. Inherited from Phase 04 human verification list."
  - test: "File watcher end-to-end loop (carried from Phase 04)"
    expected: "Running 'local-index daemon <path>' and creating/modifying/deleting a .md file produces re-indexing log lines and the index is updated"
    why_human: "Requires live file system events from notify, a real vault path, and the Voyage embedding API."
---

# Phase 04.1: SIGTERM Gap Closure Verification Report

**Phase Goal:** Close SC5 gap from Phase 04 — add SIGTERM signal handling to `src/daemon/shutdown.rs` so the daemon shuts down gracefully when process managers (systemd, launchctl, Docker) send SIGTERM, not just when Ctrl-C (SIGINT) is pressed.
**Verified:** 2026-04-10T23:00:00Z
**Status:** human_needed
**Re-verification:** Yes — after gap closure (Phase 04 status was gaps_found, score 4/5)

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | SIGTERM (sent by kill, systemd, launchctl, Docker) triggers graceful shutdown | VERIFIED | `src/daemon/shutdown.rs` line 15: `signal(SignalKind::terminate())` registered before `tokio::select!`; unit test `test_sigterm_cancels_token` sends SIGTERM via `libc::kill` and confirms token cancellation within 500ms — 2 passed |
| 2 | SIGINT (Ctrl-C) still triggers graceful shutdown as before | VERIFIED | `tokio::select!` at line 19 races `ctrl_c()` against `sigterm.recv()`; unit test `test_sigint_still_cancels_token` confirms SIGINT still cancels token — 2 passed |
| 3 | A second SIGINT still forces immediate exit | VERIFIED | `src/daemon/shutdown.rs` line 28: after token cancellation, `tokio::signal::ctrl_c().await.ok()` followed by `std::process::exit(1)` — pattern identical to original implementation |
| 4 | Whichever signal fires first cancels the CancellationToken; the other is ignored | VERIFIED | `tokio::select!` at line 19-22 is the canonical Rust pattern for racing two futures — the first to complete wins; the other branch is dropped |
| 5 | SC5 gap from Phase 04 is closed: both SIGINT and SIGTERM cancel the CancellationToken | VERIFIED | `cargo test --lib daemon::shutdown` — 2 passed (test_sigterm_cancels_token, test_sigint_still_cancels_token); `cargo test --lib daemon` — 7 passed, 0 failed |

**Score:** 5/5 truths verified

### Gap Closure Confirmation

| Phase 04 Gap | Gap Status | Closure Evidence |
|--------------|-----------|-----------------|
| SC5 (partial): shutdown.rs handles SIGINT only via ctrl_c(); no SIGTERM handler | CLOSED | `SignalKind::terminate()` at line 15; `tokio::select!` at line 19; both unit tests pass |

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/daemon/shutdown.rs` | Signal handler that races ctrl_c() against SIGTERM via tokio::select! | VERIFIED | Contains `SignalKind::terminate` (line 15), `tokio::select!` (line 19), two unit tests; function signature `pub fn setup_shutdown() -> CancellationToken` unchanged |
| `Cargo.toml` | libc = "0.2" dev-dependency for test signal delivery | VERIFIED | `libc = "0.2"` present at Cargo.toml line 85 |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `src/daemon/shutdown.rs` | `src/daemon/mod.rs` | CancellationToken returned by setup_shutdown() | VERIFIED | Phase 04 key link confirmed intact; `shutdown_token.cancel()` still called on signal receipt (line 24); mod.rs caller unchanged |

### Data-Flow Trace (Level 4)

Not applicable for this phase. The artifact modified (`shutdown.rs`) is a signal handler that produces a control-flow side effect (CancellationToken cancellation), not a data-rendering component. The unit tests directly verify the control-flow outcome.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Compile check | `cargo check` | Finished dev profile, 0 errors | PASS |
| SIGTERM cancels token (unit) | `cargo test --lib daemon::shutdown` | 2 passed, 0 failed | PASS |
| SIGINT still cancels token (unit) | `cargo test --lib daemon::shutdown` | 2 passed, 0 failed | PASS |
| Full daemon module — no regressions | `cargo test --lib daemon` | 7 passed, 0 failed | PASS |
| SignalKind::terminate symbol present | `grep -n "SignalKind::terminate" src/daemon/shutdown.rs` | Line 15 match | PASS |
| tokio::select! symbol present | `grep -n "tokio::select!" src/daemon/shutdown.rs` | Line 19 match | PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| CLI-02 | 04.1-01 | `local-index daemon <path>` command — graceful shutdown on SIGINT/SIGTERM | SATISFIED | SIGTERM handler added; both signals now cancel the CancellationToken; 7 daemon module tests pass |

### Anti-Patterns Found

No anti-patterns found. The implementation exactly matches the plan-specified code. No TODOs, no hardcoded stubs, no empty handlers. The `expect("failed to install SIGTERM handler")` at line 16 is the correct panic-at-startup pattern for critical signal registration failure.

### Human Verification Required

#### 1. SIGTERM graceful shutdown end-to-end

**Test:** Build the binary (`cargo build`), run `./target/debug/local-index daemon <vault_path>` with a valid `VOYAGE_API_KEY`, obtain the PID, and run `kill <pid>` from a second terminal.
**Expected:** The daemon logs "shutdown signal received, draining in-flight work..." and then exits cleanly with "daemon shutdown complete". No SIGKILL required.
**Why human:** Requires a running daemon process with a real vault path, VOYAGE_API_KEY, and file system events from notify. In-process unit tests confirm the CancellationToken is cancelled by SIGTERM, but the end-to-end drain-and-exit sequence requires a live binary.

#### 2. SIGINT graceful shutdown (carried from Phase 04)

**Test:** Start the daemon, press Ctrl-C once, observe logs.
**Expected:** First Ctrl-C logs "shutdown signal received, draining in-flight work..." then the process completes in-flight work and logs "daemon shutdown complete". A second Ctrl-C forces immediate exit.
**Why human:** Interactive signal delivery requires a running process.

#### 3. File watcher end-to-end loop (carried from Phase 04)

**Test:** Run `VOYAGE_API_KEY=<key> cargo run -- daemon <vault_path>`, then create/modify/delete a .md file in `<vault_path>`.
**Expected:** Each operation produces tracing log lines showing "re-indexing file", "removing chunks for deleted file", or rename variant messages. After re-index, `local-index status` shows updated chunk/file counts.
**Why human:** Requires live file system events from notify, a real vault path, and the Voyage embedding API. Cannot be reproduced with static code analysis.

### Gaps Summary

No gaps. All five observable truths are verified:

- `SignalKind::terminate` and `tokio::select!` are present in `src/daemon/shutdown.rs`
- `cargo check` compiles without errors
- `cargo test --lib daemon::shutdown` passes with 2 new tests (`test_sigterm_cancels_token`, `test_sigint_still_cancels_token`)
- `cargo test --lib daemon` passes with all 7 tests, 0 regressions
- SC5 from Phase 04 is closed: both SIGINT and SIGTERM cancel the CancellationToken

The three human verification items are end-to-end runtime tests that cannot be automated without a live binary and external dependencies (Voyage API, file system events). They are not gaps in the implementation — they are standard integration validation that requires a running environment.

---

_Verified: 2026-04-10T23:00:00Z_
_Verifier: Claude (gsd-verifier)_
