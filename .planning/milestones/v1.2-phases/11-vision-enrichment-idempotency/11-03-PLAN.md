---
phase: 11-vision-enrichment-idempotency
plan: 03
type: execute
wave: 3
depends_on:
  - "11-01"
  - "11-02"
files_modified:
  - README.md
autonomous: true
requirements:
  - PRE-13

must_haves:
  truths:
    - "README contains a subsection under the existing `## PDF and images (v1.2)` heading that explains the ephemeral `asset-cache/{shard}/{sha256}.txt` layout, cache-hit idempotency, and the corrupt-cache WARN behavior"
    - "README explicitly states that raw PDFs and images are NOT indexed as separate `.md` files (the markdown walker excludes these extensions) and that no companion `.processed.md` files are written to the vault"
    - "README documents the cache invalidation procedure (`rm -rf <data_dir>/asset-cache/`) and notes that the cache key is source SHA-256 only — changing the Anthropic model or the vision prompt will NOT invalidate existing cache entries"
    - "README describes the TextFirst embedded-image vision behavior (Phase 11): figures in text PDFs are extracted via pdfium and described via Anthropic; synthetic filename convention is `{stem}_page_{N}_image_{I}.png`"
    - "README mentions the graceful-degradation fallback: when `ANTHROPIC_API_KEY` is missing, TextFirst PDFs still index page text (image descriptions omitted with a WARN); when the system pdfium library is missing, TextFirst PDFs skip embedded-image extraction and still index text"
  artifacts:
    - path: "README.md"
      provides: "Updated v1.2 PDF and images section covering PRE-13 ephemeral-cache documentation + Phase 11 behavior"
      contains: "ephemeral"
  key_links:
    - from: "README.md `## PDF and images (v1.2)` section"
      to: "src/pipeline/assets/cache.rs (cache layout), ingest.rs (cache-read gate), pdf_images.rs (embedded-image extraction)"
      via: "documentation reference to the shipped code behavior"
      pattern: "asset-cache"
---

<threat_model>
**ASVS L1 scope**

**Trust boundaries:** none — this plan modifies only `README.md`. No code, no data flow, no new network or filesystem surface.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-11 | Information disclosure | README accidentally documents secret values or internal-only paths | mitigate | Review the new section for literal values; only document the public env var names (`ANTHROPIC_API_KEY`, `LOCAL_INDEX_DATA_DIR`) and public config paths (`asset-cache/{shard}/{sha256}.txt`). No secrets, no example keys. |
| T-11-12 | Operator confusion | README suggests a cache invalidation procedure that deletes more than intended | mitigate | Document `rm -rf <data_dir>/asset-cache/` exactly — NOT `rm -rf <data_dir>/` (which would destroy the LanceDB index). The path shown must include the `asset-cache/` segment. |

**Block on:** HIGH severity — none in a docs-only plan. T-11-12 mitigation is the acceptance-criteria grep for the exact path string.
</threat_model>

<objective>
Close **PRE-13** (documentation of the ephemeral cache approach and double-index prevention) by extending `README.md`'s existing `## PDF and images (v1.2)` section with a new subsection describing:

1. The ephemeral `asset-cache/{shard}/{sha256}.txt` layout under `<data_dir>`
2. Cache-hit idempotency (PRE-04 — re-running on unchanged source skips Anthropic/OCR API calls)
3. Corrupt-cache WARN + re-fetch behavior (D-03)
4. TextFirst PDF embedded-image vision (Phase 11 — PRE-09/PRE-10; synthetic filename convention `{stem}_page_{N}_image_{I}.png`)
5. Graceful degradation fallbacks (missing `ANTHROPIC_API_KEY`, missing pdfium system library)
6. Double-index prevention: raw PDFs and images are NOT indexed as `.md` files (walker excludes these extensions); no companion files are written to the vault (Phase 9 D-01/D-02/D-04)
7. Cache invalidation procedure: `rm -rf <data_dir>/asset-cache/` (exact path; preserves the LanceDB index)

Purpose: Complete the last remaining deliverable in v1.2 (PRE-13 documentation) so Phase 11 closes the milestone. No code change — this is a documentation-only plan, run last so the docs describe behavior that is actually shipped.

Output: one modified file (`README.md`) with a new subsection (~30-50 lines of prose + one Mermaid diagram per CLAUDE.md conventions).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-vision-enrichment-idempotency/11-CONTEXT.md
@.planning/phases/11-vision-enrichment-idempotency/11-RESEARCH.md
@.planning/phases/11-vision-enrichment-idempotency/11-01-SUMMARY.md
@.planning/phases/11-vision-enrichment-idempotency/11-02-SUMMARY.md
@README.md
@src/pipeline/assets/cache.rs
@src/pipeline/assets/ingest.rs

<interfaces>
<!-- Existing README structure the executor needs. Extracted from current README. -->

Current README.md has a `## PDF and images (v1.2)` section starting around line 103 that already mentions:
- `asset-cache/` under the configured data directory (line 105)
- `file_path` being the original PDF/image path (line 106)
- `--skip-asset-processing` flag (line 107)
- `ANTHROPIC_API_KEY` requirement for vision (line 108)
- `LOCAL_INDEX_MAX_PDF_PAGES` cap (line 109)

A sub-subsection `### OCR providers (scanned PDFs)` follows starting around line 111 and documents Anthropic vs Google Document AI.

Phase 11 adds NEW content covering idempotency, TextFirst embedded images, and the corrupt-cache behavior. The new subsection goes BETWEEN the existing top-level PDF paragraph and the existing `### OCR providers` heading.

Per CLAUDE.md conventions: any diagram MUST be Mermaid (no ASCII box-drawing).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `### Ephemeral asset cache and idempotency` subsection to README.md</name>
  <files>README.md</files>
  <read_first>
    - README.md (full file — the new subsection is inserted within the existing `## PDF and images (v1.2)` section, immediately before the `### OCR providers (scanned PDFs)` subheading)
    - .planning/phases/11-vision-enrichment-idempotency/11-CONTEXT.md (D-01 ephemeral-only constraint; D-02/D-03 cache semantics; D-07 TextFirst embedded images)
    - .planning/phases/11-vision-enrichment-idempotency/11-RESEARCH.md (Environment Availability table for pdfium fallback; Pitfall 5 for cache invalidation)
    - src/pipeline/assets/cache.rs (sharding layout `{shard}/{sha256}.txt` — confirm the exact format documented matches the code)
    - src/pipeline/assets/ingest.rs (cache-read gate behavior post-Plan 01; TextFirst branch behavior post-Plan 02 for accurate documentation)
  </read_first>
  <action>
    Edit `README.md` to insert a new subsection titled `### Ephemeral asset cache and idempotency` immediately BEFORE the existing `### OCR providers (scanned PDFs)` heading (currently around line 111). Do NOT remove or reword the existing `## PDF and images (v1.2)` paragraph (lines 103-109) or the existing `### OCR providers` section.

    The new subsection must contain — in this order — these exact load-bearing strings (enforced by grep in acceptance criteria) and the Mermaid diagram shown below:

    ```markdown
    ### Ephemeral asset cache and idempotency

    Asset preprocessing is **idempotent**. Every PDF and image is identified by
    the SHA-256 of its raw bytes; before any Anthropic or OCR API call,
    `local-index` checks an ephemeral on-disk cache and reuses the previously
    computed synthetic markdown when the source is unchanged.

    **Cache layout** — two-level shard, under the configured data directory:

    ```
    <data_dir>/asset-cache/ab/cd/{sha256}.txt
    ```

    The two-byte shards (`ab/cd/`) are the first four hex characters of the
    SHA-256 digest. The cache file contains the synthetic markdown produced for
    that exact source content — nothing more. No companion `.processed.md`
    files are ever written next to your PDFs or images in the vault.

    **Cache behavior:**

    - **Hit (file exists and is non-empty)** → re-use the cached markdown,
      skip the Anthropic/OCR API call entirely.
    - **Miss (file does not exist)** → call the API, chunk the result, then
      write the synthetic markdown to the cache on success.
    - **Corrupt (file exists but is empty or unreadable)** → emit a
      `tracing::warn!` with `corrupt_cache = true` and the path, then
      re-fetch as a cache miss and overwrite the file.

    **Cache invalidation:**

    ```sh
    rm -rf <data_dir>/asset-cache/
    ```

    The cache key is source-bytes SHA-256 only. Changing
    `LOCAL_INDEX_ASSET_MODEL` or the Anthropic vision prompt does **not**
    invalidate existing cache entries — delete `asset-cache/` to force a
    refresh after those kinds of changes.

    **Double-index prevention:** The markdown walker only indexes files with
    `.md` extension. Raw `.pdf`, `.png`, `.jpg`, `.jpeg`, and `.webp` files
    are routed exclusively through the asset pipeline — they are never
    indexed as if they were markdown. Combined with `file_path` attribution
    pointing at the original asset path, the same PDF never produces two
    sets of chunks.

    **TextFirst PDF embedded images (Phase 11):** Text-first PDFs now have
    their embedded raster figures extracted via `pdfium-render` and
    described through Anthropic vision. For each TextFirst page, the
    synthetic markdown contains the page text followed by a blockquote per
    figure using the filename convention
    `{stem}_page_{N}_image_{I}.png` (1-based page and image indices). Pages
    are separated by `\n\n---\n\n`. Scanned-PDF pages (NeedsVision) continue
    to be rasterized and OCR'd as before, with each page's OCR body wrapped
    in the same blockquote format using `{stem}_page_{N}.png`.

    **Graceful degradation:**

    - If `ANTHROPIC_API_KEY` is not set on a TextFirst PDF with embedded
      images: a WARN is logged once and the PDF indexes its page text only
      (image descriptions are omitted).
    - If the system `libpdfium` is not available: embedded-image extraction
      is skipped with a WARN; the PDF still indexes its extracted text. PDF
      *rasterization* (for scanned/NeedsVision PDFs) additionally has a
      `pdftoppm` (Poppler) fallback.

    ```mermaid
    flowchart TD
        A["ingest_asset_path(asset)"] --> B["read bytes + size cap"]
        B --> C["SHA-256(bytes) → hex"]
        C --> D{"cache file<br/>exists & non-empty?"}
        D -->|yes| E["read cached markdown"] --> F["chunk + embed"]
        D -->|no, or empty| G["WARN corrupt_cache<br/>(if empty)"]
        G --> H["classify / dispatch"]
        H -->|PDF TextFirst| I["per-page: extract text (lopdf)<br/>+ embedded images (pdfium)<br/>+ describe_image per image"]
        H -->|PDF NeedsVision| J["rasterize + OCR per page"]
        H -->|image| K["describe_image"]
        I --> L["compose synthetic markdown<br/>(blockquote per image)"]
        J --> L
        K --> L
        L --> M["tokio::fs::write cache"]
        M --> F
    ```
    ```

    Style notes:
    - Keep the inline code fences (triple-backtick) balanced. The top-level fence around the whole block above is part of THIS plan's instructions — you only write the markdown CONTENT (the prose + Mermaid) into `README.md`.
    - The Mermaid diagram uses the fenced syntax required by CLAUDE.md. Do NOT substitute ASCII box-drawing characters.
    - Word count for the new subsection: ~350-500 words. Not a novel, not a one-liner.
    - Keep existing content in the file intact — no rewording of `## PDF and images (v1.2)`, `### OCR providers (scanned PDFs)`, or any other section.

    **Anchor for insertion:** grep for the existing line `### OCR providers (scanned PDFs)` (currently around line 111). Insert the new subsection ABOVE that heading. A blank line separates the new subsection from the existing `### OCR providers` heading below.
  </action>
  <verify>
    <automated>grep -q "### Ephemeral asset cache and idempotency" README.md &amp;&amp; grep -q "asset-cache/ab/cd" README.md &amp;&amp; grep -q "corrupt_cache" README.md &amp;&amp; grep -q "Double-index prevention" README.md &amp;&amp; grep -q "_page_{N}_image_{I}" README.md &amp;&amp; grep -q "Graceful degradation" README.md &amp;&amp; grep -q '```mermaid' README.md &amp;&amp; echo README-PRE-13-OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "### Ephemeral asset cache and idempotency" README.md` is exactly 1
    - `grep -c "asset-cache/ab/cd" README.md` is at least 1 (exact shard example present)
    - `grep -c "SHA-256" README.md` is at least 1 (cache key explanation)
    - `grep -c "corrupt_cache" README.md` is at least 1 (corrupt-cache WARN documented)
    - `grep -c "rm -rf <data_dir>/asset-cache/" README.md` is at least 1 (invalidation command with exact path)
    - `grep -c "Double-index prevention" README.md` is at least 1
    - `grep -c "no companion" README.md` is at least 1 (ephemeral-only guarantee; matches "No companion" or "no companion")
    - `grep -cE "_page_\{N\}_image_\{I\}" README.md` is at least 1 (synthetic filename convention documented)
    - `grep -c "TextFirst" README.md` is at least 1 (Phase 11 TextFirst embedded-image behavior mentioned)
    - `grep -c "Graceful degradation" README.md` is at least 1
    - `grep -c "pdfium" README.md` is at least 1 (pdfium fallback documented)
    - `grep -c '```mermaid' README.md` is at least 1 (Mermaid diagram per CLAUDE.md convention)
    - Existing sections preserved: `grep -c "## PDF and images (v1.2)" README.md` is exactly 1; `grep -c "### OCR providers (scanned PDFs)" README.md` is exactly 1
    - `grep -c "ASCII" README.md` is 0 OR the existing count is unchanged (no new ASCII-box drawing added — Mermaid only)
    - The new subsection appears BEFORE the `### OCR providers (scanned PDFs)` subsection: verify with `awk '/### Ephemeral asset cache and idempotency/{a=NR} /### OCR providers \\(scanned PDFs\\)/{b=NR} END{exit !(a > 0 && b > 0 && a < b)}' README.md` exits 0
    - `cargo build -p local-index 2>&1 | tail -5` still succeeds (defensive — should be unaffected by a README change)
  </acceptance_criteria>
  <done>
    README has a new `### Ephemeral asset cache and idempotency` subsection placed between the existing top-level PDF paragraph and the OCR providers subsection. It documents: cache layout (`asset-cache/{shard}/{sha256}.txt`), cache-hit idempotency, corrupt-cache WARN, cache invalidation procedure, the "source SHA-256 only" key limitation, double-index prevention (walker exclusion + no companion files), TextFirst embedded-image vision behavior with the `{stem}_page_{N}_image_{I}.png` convention, and graceful-degradation fallbacks for missing `ANTHROPIC_API_KEY` and missing pdfium. Contains one Mermaid flowchart (per CLAUDE.md Diagrams convention). All acceptance-criteria greps match.
  </done>
</task>

</tasks>

<verification>
**Plan 03 regression sweep:**

- `cargo build -p local-index 2>&1 | tail -5` — succeeds (README change cannot break a build but sanity-check anyway)
- `cargo test --all-targets 2>&1 | tail -10` — all tests from plans 01/02 still green

**Doc review:** A human reading the new section should understand (1) re-running the preprocessor on an unchanged source is free, (2) how to force a re-fetch, (3) why they do not see `.processed.md` files in their vault, (4) what happens if pdfium or `ANTHROPIC_API_KEY` is missing.
</verification>

<success_criteria>
- PRE-13: README documents the ephemeral-cache approach, double-index prevention, and all Phase 11 behavior (idempotency, TextFirst embedded-image vision, graceful degradation)
- CLAUDE.md conventions: Mermaid diagram used for the new flowchart (no ASCII box-drawing)
- No code change; no Cargo.toml change; no test change
- Existing README sections (Quick Start, Architecture, CLI Reference, `## PDF and images (v1.2)`, `### OCR providers`) are preserved verbatim
- The `v1.2 (SEED-001)` milestone is now fully documented; PRE-13 can be marked complete in REQUIREMENTS.md (done by `/gsd-verify-work` or orchestrator, not this plan)
</success_criteria>

<output>
After completion, create `.planning/phases/11-vision-enrichment-idempotency/11-03-SUMMARY.md` per the template at `$HOME/.claude/get-shit-done/templates/summary.md`.
</output>
