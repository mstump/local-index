---
phase: 10-ocr-providers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pipeline/assets/ocr.rs
  - src/pipeline/assets/mod.rs
  - src/pipeline/assets/ingest.rs
  - src/main.rs
  - src/daemon/mod.rs
  - src/daemon/processor.rs
autonomous: true
requirements:
  - PRE-07

must_haves:
  truths:
    - "NeedsVision PDF branch calls a dedicated OCR dispatch type (`OcrService`) instead of calling `AnthropicAssetClient::describe_raster_page` directly"
    - "Standalone raster images (`png`/`jpg`/`jpeg`/`webp`) still use `AnthropicAssetClient::describe_image` unchanged from Phase 9 semantics"
    - "When `LOCAL_INDEX_OCR_PROVIDER` is unset or `anthropic`, rasterized scanned PDF pages still produce non-empty markdown body via Anthropic vision (same prompt `ASSET_VISION_PROMPT`)"
  artifacts:
    - path: "src/pipeline/assets/ocr.rs"
      provides: "OcrService enum with Anthropic variant + ocr_scanned_pdf_pages helper"
      contains: "OcrService"
    - path: "src/pipeline/assets/ingest.rs"
      provides: "NeedsVision uses OcrService; images use AnthropicAssetClient"
      contains: "NeedsVision"
  key_links:
    - from: "src/pipeline/assets/ocr.rs"
      to: "src/pipeline/assets/anthropic_extract.rs"
      via: "delegates Anthropic variant to describe_raster_page"
      pattern: "describe_raster_page"
---

<threat_model>
**ASVS L1 scope (third-party APIs)**

| ID | Threat | Severity | Mitigation in this plan |
|----|--------|----------|---------------------------|
| T-10-01 | API key in logs | High | Do not add `tracing::info!` that prints OCR request bodies or keys; keep existing Anthropic logging discipline |
| T-10-01b | TLS MITM | High | Continue `reqwest` + `rustls-tls` only |

**Block on:** High severity credential/TLS regressions.

</threat_model>

<objective>
Introduce `OcrService` (enum with **Anthropic** variant only in this plan) and refactor the **NeedsVision PDF** path in `ingest.rs` to OCR per-page PNGs through it. Wire **call sites** so behavior matches Phase 9 when provider is Anthropic (PRE-07: rasterize → OCR path).

This plan does **not** add Google Document AI or new CLI flags — those are **10-02**.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-ocr-providers/10-CONTEXT.md
@.planning/phases/10-ocr-providers/10-RESEARCH.md
@src/pipeline/assets/ingest.rs
@src/pipeline/assets/anthropic_extract.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `ocr.rs` with Anthropic-backed OCR dispatch</name>
  <files>src/pipeline/assets/ocr.rs, src/pipeline/assets/mod.rs</files>
  <read_first>src/pipeline/assets/anthropic_extract.rs, src/pipeline/assets/mod.rs, src/error.rs</read_first>
  <action>
Create `src/pipeline/assets/ocr.rs` and export from `assets/mod.rs`.

Define:

```rust
pub enum OcrService {
    Anthropic(AnthropicAssetClient),
}
```

Implement:

- `impl OcrService { pub async fn ocr_scanned_pdf_pages(&self, png_pages: &[Vec<u8>]) -> Result<Vec<String>, LocalIndexError> }`

Behavior for `Anthropic` variant: for each page buffer, call `describe_raster_page` (same as current `ingest.rs` loop order). Return `Vec<String>` with one string per page (may be joined by caller).

No `async_trait` crate — enum dispatch only.

Add module-level doc comment stating **standalone images are not routed here** (Phase 10 CONTEXT D-01).
  </action>
  <verify>
    <automated>cd /Users/matthewstump/src/local-index && cargo test -p local-index -- pipeline::assets 2>&1</automated>
  </verify>
  <acceptance_criteria>
- `rg "enum OcrService" src/pipeline/assets/ocr.rs` matches
- `rg "ocr_scanned_pdf_pages" src/pipeline/assets/ocr.rs` matches
- `rg "mod ocr" src/pipeline/assets/mod.rs` matches
- `cargo check -p local-index` exits 0
  </acceptance_criteria>
  <done>OcrService Anthropic variant compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor NeedsVision branch to use `OcrService`</name>
  <files>src/pipeline/assets/ingest.rs</files>
  <read_first>src/pipeline/assets/ingest.rs, src/pipeline/assets/ocr.rs</read_first>
  <action>
Change `ingest_asset_path` signature from:

`client: Option<&AnthropicAssetClient>`

to accept **both**:

- `pdf_ocr: Option<&OcrService>` — required for NeedsVision branch when classification is `NeedsVision`
- `image_vision: Option<&AnthropicAssetClient>` — used only for standalone image extensions

In the `PdfClassification::NeedsVision` branch:

- If `pdf_ocr` is `None`, return the same `LocalIndexError::Credential` style error as today, but message must mention scanned PDF OCR (not only `ANTHROPIC_API_KEY` if we later support Google — use wording like “no OCR provider configured for scanned PDFs” while still instructing to set `ANTHROPIC_API_KEY` when using default Anthropic-only installs).

- Call `pdf_ocr.ocr_scanned_pdf_pages(&pngs).await?` then `parts.join("\n\n---\n\n")` exactly as today.

Leave **standalone image** branch calling `image_vision.describe_image(...)` unchanged in behavior.

Update `#[cfg(test)]` test `text_first_pdf_chunks_use_asset_path` to pass `None` for both new params where appropriate (text-first PDF needs neither OCR nor image client).

Add a **new** unit test using existing Phase 9 fixtures: construct `OcrService::Anthropic(AnthropicAssetClient::new_for_test(...))` with wiremock OR use `new_for_test` pointing at mock — minimal test: **mock server returns text for one PNG** and assert joined body non-empty. If full wiremock is heavy, add `#[tokio::test]` that uses `AnthropicAssetClient::new_for_test` + `wiremock::MockServer` mirroring `tests/anthropic_assets_mock.rs` pattern for **one** `describe_raster_page` round-trip through `OcrService::Anthropic`.

At minimum: **one** async test proving `OcrService::Anthropic` calls into vision path (reuse mock from existing tests if possible).
  </action>
  <verify>
    <automated>cd /Users/matthewstump/src/local-index && cargo test -p local-index ingest 2>&1</automated>
  </verify>
  <acceptance_criteria>
- `rg "pdf_ocr" src/pipeline/assets/ingest.rs` matches
- `rg "image_vision" src/pipeline/assets/ingest.rs` matches
- `rg "ocr_scanned_pdf_pages" src/pipeline/assets/ingest.rs` matches
- `cargo test -p local-index ingest` exits 0
  </acceptance_criteria>
  <done>NeedsVision uses OcrService; tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Wire main + daemon call sites</name>
  <files>src/main.rs, src/daemon/mod.rs, src/daemon/processor.rs</files>
  <read_first>src/main.rs, src/daemon/mod.rs, src/daemon/processor.rs, src/pipeline/assets/ocr.rs</read_first>
  <action>
**main.rs** (`index` command asset loop): When `AnthropicAssetClient::new_from_env()` succeeds, wrap as `Some(OcrService::Anthropic(client))` for `pdf_ocr`, and pass `Some(&anthropic)` for `image_vision` (same instance). When env missing, pass `None` for both (preserves failures for assets needing vision).

**daemon:** Update `Arc` types / `run_event_processor` signature: replace single `anthropic: Option<Arc<AnthropicAssetClient>>` with a small struct **or** pass both `Option<Arc<AnthropicAssetClient>>` for images and `Option<Arc<OcrService>>` for PDF OCR — **prefer struct** `Arc<AssetClientsInner>` `{ anthropic: AnthropicAssetClient }` where `pdf_ocr` is always `OcrService::Anthropic(same)` when present to avoid double `Arc`.

Simplest acceptable shape: keep `Option<Arc<AnthropicAssetClient>>` at daemon boundary but map at call site to `pdf_ocr: Some(OcrService::Anthropic((*arc).clone()))` and `image_vision: Some(&*arc)` — `AnthropicAssetClient` is `Clone`.

Ensure every `ingest_asset_path` call compiles with updated arity.

Run `cargo test` full suite.
  </action>
  <verify>
    <automated>cd /Users/matthewstump/src/local-index && cargo test -p local-index 2>&1</automated>
  </verify>
  <acceptance_criteria>
- `rg "ingest_asset_path" src/main.rs src/daemon/processor.rs` shows updated argument lists
- `cargo test -p local-index` exits 0
  </acceptance_criteria>
  <done>CLI + daemon wired; full tests green.</done>
</task>

</tasks>

---

## PLANNING COMPLETE
